# This is a standalone project, independent of any parent pnpm workspace.
# Without this, running `pnpm install` inside a repo checkout (e.g. when the
# project lives under <repo>/.od/projects/...) resolves to the parent
# workspace root and leaves this project's node_modules empty.
ignore-workspace=true
