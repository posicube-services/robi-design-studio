// Tailwind v4 runs as a PostCSS plugin; there is no tailwind.config.js because
// the theme comes from the design system's `tailwind-v4.css` @theme block.
export default { plugins: { '@tailwindcss/postcss': {} } };
