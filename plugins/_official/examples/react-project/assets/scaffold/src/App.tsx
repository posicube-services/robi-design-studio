// Seed entry component. Replace this with the real screens/components for the
// brief — split each meaningful UI unit into its own file under
// `src/components/` with typed props, and drive all styling from the design
// system tokens in `src/styles/tokens.css` (via `var(--*)`).
export default function App() {
  return (
    <main className="app-shell">
      <h1>React Project</h1>
      <p>
        Replace this seed with your design. Tokens live in{' '}
        <code>src/styles/tokens.css</code>.
      </p>
    </main>
  );
}
